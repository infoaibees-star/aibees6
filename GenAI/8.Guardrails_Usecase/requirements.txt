# Core — required for every backend
streamlit>=1.35
python-dotenv>=1.0.1
langchain-core>=0.3
langchain-community>=0.3
langchain-text-splitters>=0.3

# Document loading (PyMuPDF is preferred; pypdf is the fallback)
pymupdf>=1.24
pypdf>=4.2

# Vector store — required for RAG_BACKEND=vertexai and RAG_BACKEND=ollama
faiss-cpu>=1.8

# RAG_BACKEND=vertexai (default)
langchain-google-vertexai>=2.0.7
google-cloud-aiplatform>=1.50

# RAG_BACKEND=ollama — remove if you never run locally
langchain-ollama>=0.2
httpx>=0.27
